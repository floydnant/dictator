<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Platform-NEUTRAL. CA1416 makes any Windows-only API call here a build error,
         which is what keeps this layer testable from macOS. -->
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Dictator.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Dictator.Abstractions\Dictator.Abstractions.csproj" />
    <ProjectReference Include="..\Dictator.Dictionary\Dictator.Dictionary.csproj" />
  </ItemGroup>
</Project>
